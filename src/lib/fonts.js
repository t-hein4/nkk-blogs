import { Assistant, Montserrat, Dancing_Script } from '@next/font/google';
export const general = Assistant({
  subsets: ['latin'],
});

export const heading = Montserrat({
  subsets: ['latin'],
});

export const logo = Dancing_Script({
  subsets: ['latin'],
});
